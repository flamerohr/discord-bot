version: '3.8'

services:

    bot:
      build: ./bot
      command: run start
      volumes:
        - .:/usr/node

    chatroom:
      build: ./chat-room
      command: run start
      ports:
        - "3000:3000"

    chatroomsocketioserver:
      build: ./chat-room-socketio-server
      command: run start
      ports:
        - "8000:8000"
